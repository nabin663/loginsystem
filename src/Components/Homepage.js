import React from "react";
import Header from "./Header";
import Logout from "./Logout";

const Homepage = () => {
  return (
    <div>
      <Header />
      <Logout />
    </div>
  );
};

export default Homepage;
